# TODO
class GAEngine():
    def __init__(self, crossover = None, mutation = None, selection = None):
        self.crossover = crossover
        self.mutation = mutation
        self.selection = selection

    def evolve(self):

if "__main__" == __name__:
    ga1 = GAEngine(crossover = PMX)
    ga2 = GAEngine(crossover = OX)
    ga3 = GAEngine(crossover = PMX)
    ga4 = GAEngine(crossover = OX)
